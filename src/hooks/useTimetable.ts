/**
 * Convenience re-export so features can import from either:
 *   import { useTimetable } from '../hooks/useTimetable'
 * or the canonical context path:
 *   import { useTimetable } from '../context/TimetableContext'
 */
export { useTimetable } from '../context/TimetableContext';
